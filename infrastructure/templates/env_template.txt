# ============================================================
# КОНФИГУРАЦИЯ ИНФРАСТРУКТУРНОГО СЛОЯ
# ============================================================
# Этот файл по умолчанию имеет имя .infra.env.
# Вы можете переопределить имя файла через переменную окружения INFRA_ENV_FILE.
# Переменные окружения имеют приоритет над значениями из этого файла.
# ============================================================

# --- ПОДКЛЮЧЕНИЕ К БАЗЕ ДАННЫХ ---

# Sync-драйвер PostgreSQL: psycopg (по умолчанию) или psycopg2 (legacy)
# Если DB_URL задан без явного драйвера (postgresql://...), будет подставлен этот драйвер
DB_SYNC_DRIVER=psycopg

# Синхронный URL (используется для миграций)
DB_URL=postgresql+psycopg://postgres:123@localhost:5432/Proj_v2

# Асинхронный URL (используется для работы event_infrastructure)
DB_URL_ASYNC=postgresql+asyncpg://postgres:123@localhost:5432/Proj_v2

# БД для тестирования
TEST_DB_URL=postgresql+psycopg://postgres:123@localhost:5432/Proj_v2
TEST_DB_URL_ASYNC=postgresql+asyncpg://postgres:123@localhost:5432/Proj_v2

# --- ЛОГИРОВАНИЕ ---

# Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# --- КАНАЛЫ ---

# Список имён каналов через запятую (без пробелов).
# Можно добавлять любые свои каналы.
CHANNELS=read,write,admin

# Для каждого канала можно задать параметры с префиксом ИМЯ_КАНАЛА_ (в верхнем регистре).
# Если параметр не задан, используются значения по умолчанию (pool_size=10, max_overflow=5, queue_maxsize=1000).

# --- КАНАЛ read ---
READ_POOL_SIZE=39
READ_MAX_OVERFLOW=10
READ_QUEUE_MAXSIZE=50000
# Количество воркеров (0 = pool_size)
# READ_WORKERS=20

# --- КАНАЛ write ---
WRITE_POOL_SIZE=60
WRITE_MAX_OVERFLOW=5
WRITE_QUEUE_MAXSIZE=50000
# WRITE_WORKERS=10

# --- КАНАЛ admin ---
ADMIN_POOL_SIZE=1
ADMIN_MAX_OVERFLOW=0
ADMIN_QUEUE_MAXSIZE=5000
# ADMIN_WORKERS=1

# --- Пример добавления нового канала ---
# Если вы добавили канал report, задайте его параметры:
# REPORT_POOL_SIZE=5
# REPORT_MAX_OVERFLOW=2
# REPORT_QUEUE_MAXSIZE=100
# REPORT_WORKERS=3


# --- ОБЩИЕ ПАРАМЕТРЫ ПУЛА СОЕДИНЕНИЙ ---

# Время жизни соединения в секундах (пересоздание)
POOL_RECYCLE=3600

# Проверять соединение перед использованием (SELECT 1)
POOL_PRE_PING=True

# Таймаут ожидания соединения из пула (сек)
POOL_TIMEOUT=30


# --- ПАРАМЕТРЫ ВЫПОЛНЕНИЯ ЗАДАЧ ---

# Таймаут выполнения задачи по умолчанию (сек)
DEFAULT_TIMEOUT=30.0

# Максимальное количество одновременно выполняемых операций (0 - без ограничений)
MAX_CONCURRENCY=100000

# Таймаут graceful shutdown (сек)
SHUTDOWN_TIMEOUT=10.0


# --- ПОВТОРНЫЕ ПОПЫТКИ (RETRY) ---

# Максимальное число повторных попыток при ошибках соединения
RETRY_MAX_RETRIES=3

# Начальная задержка между попытками (сек)
RETRY_DELAY_SECONDS=0.5

# Множитель увеличения задержки (экспоненциальная задержка)
RETRY_BACKOFF_MULTIPLIER=2.0

# Общий таймаут всех повторных попыток (сек, 0 = без ограничений)
RETRY_MAX_TOTAL_TIMEOUT=0.0

# Повтор при timeout (по умолчанию False — мутации create/update/delete не ретраятся при timeout)
# Для read операций retry при timeout работает всегда (идемпотентные операции)
RETRY_ON_TIMEOUT=false


# --- КЕШИРОВАНИЕ ---

# Включить кеширование ответов на чтение
CACHE_ENABLED=True

# Время жизни кеша (сек)
CACHE_TTL_SECONDS=3.0

# Максимальное количество записей в кеше
CACHE_MAX_SIZE=5000


# --- ТАБЛИЦЫ ДЛЯ ИГНОРИРОВАНИЯ ---

# Список таблиц через запятую, которые не участвуют в миграциях и CRUD
# (по умолчанию alembic_version)
EXCLUDE_TABLES=alembic_version
